@using MudBlazor
<MudTextField Required="@true" FullWidth="true" @bind-Value="Poster.name" Label="Postername" />
<!-- <MudDatePicker Required="@true" ShowWeekNumbers="true" Label="Start date" MinDate="DateTime.Now"
    MaxDate="Poster.endDate" @bind-Date="Poster.startDate" DateFormat="dd/MM/yyyy" />
<MudDatePicker Required="@true" ShowWeekNumbers="true" Label="End date" MinDate="Poster.startDate"
    @bind-Date="Poster.endDate" DateFormat="dd/MM/yyyy" /> -->
<MudTextField Required="@true" FullWidth="true" @bind-Value="Poster.image"
    Label="Image url" />

@code
{
    [Parameter]
    public Poster Poster { get; set; }
}